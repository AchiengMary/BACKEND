services:
  - type: web
    name: f1-backend
    env: docker
    dockerfilePath: Dockerfile 
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: "8000"
          # - key: ENV
          #  value: production
